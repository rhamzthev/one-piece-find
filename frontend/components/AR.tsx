import { Text } from "react-native"
import {
    ViroARScene,
    ViroText,
    ViroTrackingStateConstants,
    ViroARSceneNavigator,
    ViroTrackingReason,
  } from "@reactvision/react-viro";

export default function AR() {

    console.log(ViroText);

    return (
        <Text>AR</Text>
    )
}